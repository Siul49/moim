/**
 * E2E 테스트 — 참여자 플로우 (스켈레톤)
 *
 * PRD 3.2~3.3의 참여자 플로우를 실제 브라우저에서 검증.
 * UI 페이지 구현 후 작성 예정.
 */

// import { test, expect } from '@playwright/test'
//
// test('참여자가 링크를 통해 가용시간을 입력할 수 있다', async ({ page }) => {
//   await page.goto('/schedule/test-session-id')
//   // TODO: 시간 그리드 클릭 → 가용시간 제출 검증
// })
