/**
 * E2E 테스트 — 호스트 플로우 (스켈레톤)
 *
 * PRD 3.1의 호스트 플로우를 실제 브라우저에서 검증.
 * UI 페이지 구현 후 작성 예정.
 */

// import { test, expect } from '@playwright/test'
//
// test('호스트가 새 일정을 생성할 수 있다', async ({ page }) => {
//   await page.goto('/schedule/create')
//   // TODO: 일정 생성 폼 입력 → 공유 링크 생성 검증
// })
