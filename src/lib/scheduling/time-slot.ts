/**
 * 시간 슬롯 유틸리티 (스켈레톤)
 *
 * TimeSlot 관련 헬퍼 함수들.
 * 정렬, 병합, 빈 시간 추출 등.
 *
 * TODO: TDD로 구현 예정
 */

import type { TimeSlot } from "@/types/schedule";

/** 슬롯 배열을 요일→시작시간 순으로 정렬 */
export function sortSlots(_slots: TimeSlot[]): TimeSlot[] {
  // TODO: TDD — 테스트 먼저 작성 후 구현
  throw new Error("Not implemented yet");
}

/** 겹치는 슬롯을 하나로 병합 */
export function mergeOverlapping(_slots: TimeSlot[]): TimeSlot[] {
  // TODO: TDD — 테스트 먼저 작성 후 구현
  throw new Error("Not implemented yet");
}
